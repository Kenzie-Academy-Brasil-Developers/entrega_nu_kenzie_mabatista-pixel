import { logo } from "../../assets/logo.svg"

export const Header = () => {
    return(
        <header>
            <div>
                <img src={logo} alt="Nu Kenzie Logo" />
            </div>
        </header>
    )
}